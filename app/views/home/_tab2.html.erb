 <% items_array.each_with_index do |items,i| %>
       <% if items.nil? %>
       <% else%>
         <div class="row" >
            <label class="control-label">▪️<%= categories[i].category_name %> </label>
         </div>  <!-- row category名 -->
         <br>
         <div class="row tab2_category_<%= categories[i].id %>">
                <%= render partial: 'tab2_category', locals: {items: items} %> 
         </div>   <!-- row item -->
      <% end %>  <!-- if end -->
       <div class = "row tab2_category_<%= categories[i].id %>_paginate">
           &emsp;&emsp;<%= paginate items, params: {:tab => "tab2",:category_id => categories[i].id}, :remote => true %>
                   </div> 
            
 <% end %> 
 