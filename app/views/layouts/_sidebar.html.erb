
<div class= 'profile'>
    <div class= 'container'>
        <nav class = "navbar bg-warning navbar-dark box-header row ">
          <a class = "navbar-brand" href="/user/profile">プロフィール</a>
          <% if user == current_user %>
          <p><%= link_to '編集', user_profile_path %></p>
          <% else %>
          <p><%= link_to '詳細', user_path %></p>
          <% end %>
        </nav>
   <div class='row'>
        <div class="col-md-12">
             <div class= "card p-4">
                   <%= image_tag user.avatar.url, class: "card-img-top rounded-circle"%>
             </div>
        </div>
   <div class="col-md-12">
      <table class="table table-striped table-condensed ">
      
      <tr><td>名前:</td></tr>
      <tr><td><%= user.public_name %></td></tr>
      <tr><td>エリア:</td></tr> 
      <tr><td><%= user.area1 %> / <%= user.area2 %></td></tr>
      <tr><td>家族構成:</td></tr>
      <tr><td><%= user.family %></td></tr>
      <% if remindmails.nil? %>
      <% else%>
        <% remindmails.each_with_index do |remindmail, i| %>
             <tr><td><%= "メールアドレス#{i+1}"%>:</td></tr> 
             <tr><td><%= remindmail.remind_email %></td></tr>
        <% end %>
       <% end %>
      
       <%if user.role == "admin" %>
        <tr><td>管理者用</td></tr>
        <tr><td><%= link_to "アドミンページ", adminhome_path %></td></tr>
      <% end %>
     </table>
    </div>  
</div>  <!-- col-mid-12 -->
</div>
</div>