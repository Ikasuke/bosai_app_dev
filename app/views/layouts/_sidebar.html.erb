
<div class= 'profile'>
    <div class= 'container'>
        <nav class = "navbar bg-warning navbar-dark box-header row ">
          <a class = "navbar-brand" href="/user/profile">プロフィール</a>
          <% if user == current_user %>
          <p><%= link_to '編集', user_profile_path %></p>
          <% else %>
          <p><%= link_to 'ユーザーページ', user_path(id:user.id), style: "font-size: 12px;" %></p>
          <% end %>
        </nav>
   <div class='row'>
        <div class="col-md-12">
           <%= link_to user_path(id:user.id) do %>
             <div class= "card p-4">
                   <%= image_tag user.avatar.url, class: "card-img-top rounded-circle"%>
             </div>
             <% end %>
        </div>
   <div class="col-md-12">
      <table class="table table-striped table-condensed ">
      
      <tr><td>ニックネーム:</td></tr>
      <tr><td>
       <% if user.public_name.blank? %>
        <span class="yet_blank"> ニックネームを記入してください <span>
       <% else %>
         <%= user.public_name %>
       <% end %>
      </td></tr>
      <tr><td>エリア:</td></tr> 
      <tr><td>
       <% if user.area1.blank? %>
         <span class="yet_blank"> エリアを記入してください<span>
       <% else %>
         <%= user.area1 %> / <%= user.area2 %>
       <% end %>
      </td></tr>
      <tr><td>家族構成:</td></tr>
      <tr><td>
       <% if user.family.blank? %>
        <span class="yet_blank"> 家族構成を記入してください <span>
       <% else %>
          <%= user.family %>
       <% end %>
      </td></tr>
      <tr><td>自己紹介:</td></tr>
      <tr><td>
        <% if user.profile.blank? %>
         <span class="yet_blank"> 自己紹介を記入してください</span>
       <% else %>
         <p style="font-size: 12px;"> <%= user.profile %> </p>
       <% end %>
      </td></tr>
      <% if remindmails.nil? %>
      <% else%>
        <% remindmails.each_with_index do |remindmail, i| %>
             <tr><td><%= "メールアドレス#{i+1}"%>:</td></tr> 
             <tr><td><%= remindmail.remind_email %></td></tr>
        <% end %>
       <% end %>
     </table>
    </div>  
</div>  <!-- col-mid-12 -->
</div>
</div>