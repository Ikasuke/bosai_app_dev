// お気に入りonでカウント処理&ボタンをいいね削除へ変更

$('.count_favorite<%= @favorite.to_user_id %>').html("<%= @count %>");

$('.favorite_btn_<%= @favorite.to_user.id %>').html(
   '<%=button_to favorite_path(@favorite.id), method: :delete, class: 'off-btn',remote: true do %>取り消す<% end %>'
);

console.log("favorite/creste.js.erb_お気に入りボタン処理");